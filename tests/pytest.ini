[pytest]
# These run time arguments are always taken into consideration while running the tests. They act as enabled by default.
# They can however be overridden when tests are run. This assumes you're in tests folder before running.
addopts = -v -s --alluredir ../allure-results --disable-warnings